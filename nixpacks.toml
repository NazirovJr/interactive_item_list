[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "npm install --omit=dev",
  "cd server && npm install --omit=dev",
  "cd /app/client && npm install --omit=dev"
]

[start]
cmd = "npm run start" 